name: validate-nc

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm test

  sarif:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      # bracket is expected to block (exit 1); we still want the SARIF uploaded.
      - name: Validate bracket example to SARIF
        run: npx tsx cli/sarif.ts public/examples/bracket.nc public/examples/bracket.workcell.json public/examples/bracket.state.json bracket.sarif
        continue-on-error: true
      # clean must pass: guards against regressions in pass-detection.
      - name: Assert clean example passes
        run: npx tsx cli/main.ts check public/examples/clean.nc --workcell public/examples/clean.workcell.json --state public/examples/clean.state.json
      - name: Upload SARIF
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: bracket.sarif
